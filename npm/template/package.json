{
  "name": "${PACKAGE_NAME}",
  "version": "${VERSION}",
  "description": "An ACP-compatible coding agent powered by Codex - ${OS} ${ARCH} binary",
  "license": "Apache-2.0",
  "author": "lody <agent@lody.ai>",
  "homepage": "https://github.com/Leeeon233/acp-extension",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Leeeon233/acp-extension.git"
  },
  "bin": {
    "${PACKAGE_NAME}": "bin/acp-extension-codex"
  },
  "scripts": {
    "prepack": "node ./ensure-executable.mjs",
    "postinstall": "node ./ensure-executable.mjs"
  },
  "os": [
    "${OS}"
  ],
  "cpu": [
    "${ARCH}"
  ],
  "files": [
    "bin",
    "ensure-executable.mjs"
  ]
}
